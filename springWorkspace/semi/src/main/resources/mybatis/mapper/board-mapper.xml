<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kh.semi.board.mapper.BoardMapper">
    <insert id="insertBoardAttachment">
        INSERT ALL
        <foreach collection="list" item="x">
        INTO BOARD_ATTACHMENT (NO,REF_NO,CHANGE_NAME) VALUES((SELECT GET_BAROD_ATTACHMENT_NO FROM DUAL), SEQ_BOARD.CURRVAL, #{x})
        </foreach>
        SELECT 1 FROM DUAL
    </insert>

    <insert id="updateBoardAttachment">
        INSERT ALL
        <foreach collection="changeNameList" item="x">
            INTO BOARD_ATTACHMENT (NO,REF_NO,CHANGE_NAME) VALUES((SELECT GET_BAROD_ATTACHMENT_NO FROM DUAL), #{no}, #{x})
        </foreach>
        SELECT 1 FROM DUAL
    </insert>

    <select id="list">
        SELECT
        B.NO
        , B.TITLE
        , B.CONTENT
        , B.CATEGORY_NO
        , C.NAME        AS CATEGORY_NAME
        , B.WRITER_NO
        , M.NICK        AS WRITER_NICK
        , B.HIT
        , B.CREATE_DATE
        , B.DEL_YN
        FROM BOARD B
        JOIN MEMBER M ON (B.WRITER_NO = M.NO)
        JOIN CATEGORY C ON (B.CATEGORY_NO = C.NO)
        WHERE B.DEL_YN='N'

        <if test="searchType == 'title'">
            AND TITLE LIKE '%${searchValue}%'
        </if>
        <if test="searchType == 'category'">
            AND CATEGORY_NO = #{searchValue}
        </if>

        ORDER BY NO DESC
        OFFSET #{pvo.offset} ROWS FETCH NEXT #{pvo.boardLimit} ROWS ONLY
    </select>

    <select id="getBoardCnt">
        SELECT COUNT(NO)
        FROM BOARD
        WHERE DEL_YN = 'N'
        <if test="searchType == 'title'">
            AND TITLE LIKE '%${searchValue}%'
        </if>
        <if test="searchType == 'category'">
            AND CATEGORY_NO = #{searchValue}
        </if>
    </select>

</mapper>

