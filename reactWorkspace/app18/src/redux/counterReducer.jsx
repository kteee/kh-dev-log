// const initState = { value: 100 };

// const counterReducer = (state = initState, action) => {
//   switch (action.type) {
//     case 'plus':
//       return { ...state, value: state.value + 1 };
//     case 'minus':
//       return { ...state, value: state.value - 1 };
//     default:
//       return state;
//   }
// };

// export default counterReducer;
